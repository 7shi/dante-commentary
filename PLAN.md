# 構想

最終的に、**『神曲』の原文全体に注釈を埋め込んだ読み物を GitHub Pages で公開する**ことを目指す。

## 現状

生成物は歌ごとに2つある。

- `<NN>.md` — 解説記事。場面ごとに見出しを立て、要点となる行を引用して寓意・背景を論じる
- `<NN>.txt` — 対訳。全行について原文と日本語訳を1行ずつ交互に並べる

この2つは補い合う関係にあるが、分かれているために不便がある。

- 解説は引用が抜粋であり、全体を通して読めない。被覆率はモデルによって大きく違う（astra は136行中73行、gemma4-26b は13行）
- 対訳は全行あるが、なぜその訳語を選んだのかは解説を見なければ分からない。実際 `raggi del pianeta`（17行）の「天体の光」や `color che son contenti nel foco`（118行）の「苦しみを受け入れている者たち」は、解説の注釈と対で読んで初めて意図が分かる
- 結果として、読者は2つのファイルを行き来する必要がある

## 目指す形

歌ごとに1ページ。**原文全行を通して読める本文**を軸に、そこへ注釈を埋め込む。

原文と日本語訳は横に並べた対訳とする。行番号・原文・訳を1行として、原文が左、訳が右に来る2カラム。行番号はアンカーにして相互参照できるようにする（他の歌からの言及、解説からの参照）。

ページ全体は、この対訳を行範囲で区切った `div` を縦に積んだ構造になる。区切りは**注釈の有無**で決める。

```
┌─────────────────────────────────────┐
│ 1 Nel mezzo del cammin…  人生の道の半ばで、 │  注釈のある範囲
│ 2 mi ritrovai per…       ある暗い森の中に…  │
│ 3 ché la diritta via…    まっすぐな道が…    │
├─────────────────────────────────────┤
│ ## 人生の半ばの「暗い森」                    │
│ 冒頭で重要なのは…（寓意・背景の解説）          │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│ 4 Ahi quanto a dir…     ああ、どのような…   │  注釈のない範囲
│ 5 esta selva selvaggia… あの荒々しく…      │  （対訳のみ）
│ 6 che nel pensier…      思い返すだけで…    │
└─────────────────────────────────────┘
```

- 注釈のある範囲は、対訳に続けて見出しと解説を持つ
- 注釈のない範囲は、対訳だけを淡く表示して読み流せるようにする
- 語句レベルの注釈は該当行に紐づける（`veltro`、`tra feltro e feltro`、`sub Iulio` など）

**範囲は歌全体を隙間なく覆う。** 解説が引用しているのは飛び飛びの行なので、注釈のある範囲どうしの間が空く。その間は注釈のない範囲として連続的に補完し、どの行も必ずいずれかの `div` に属するようにする。これにより、注釈を追う読み方と、原文を通しで読む読み方の両方が同じページで成立する。

## 再編集の工程

1. 解説記事から見出しと解説段落を取り出し、それぞれが担当する行範囲を引用行番号から決定する
2. 1で埋まらなかった行を、注釈のない範囲として補完する。隣接する未割当行をまとめ、歌全体が隙間なく範囲に分割された状態にする
3. 各範囲について、対訳と（あれば）見出し・解説を持つ `div` を組み立てる
4. 語句注を抽出し、該当行に紐づける

注釈のない範囲が長く続く箇所は、その範囲の解説を追加生成して埋めることもできる。ただし解説を全行に付けるのが目的ではないので、必須ではない。

## 後処理

生成時ではなく、後処理で機械的に直すべきものがある。1行ずつ訳す方式では、行を跨いだ情報が保持できないためである。

### 鍵括弧の整合

台詞の範囲は複数行にわたるため、1行単位の翻訳では開始と終了が保持されない。実際、astra の出力では93行で開いた台詞が96行末で閉じられているが、ウェルギリウスの発言は111行まで続く。67〜78行も一続きの発言でありながら75行で一度閉じている。

原文には `«` `»` が入っているので、これを手掛かりに機械的に処理できる。

- 各行の原文について `«` `»` の出現を数え、台詞の開始行・終了行・継続中の行を確定する
- 訳文側の `「` `」` を除去した上で、原文の状態に従って再構成する
- 台詞が入れ子になる場合（引用中の引用）は `『` `』` を充てる

LLMを使う必要はなく、上位モデルであっても同じ誤りが出るので、パイプラインの一部として組み込むのが妥当である。

### その他の候補

- **隣接行の重複検出**: 同じ内容が2行以上に繰り返される崩れ方は、訳文どうしの類似度で検出できる。小さいモデルほど頻発する
- **用語の統一**: 固有名詞の表記揺れ（ヴェルトロ／ヴォルトロなど）を歌をまたいで統一する
- **未訳・複写の検査**: 現在 `scripts/generate.py` が行っている検査（プレースホルダーの残存、原文の複写、日本語文字の有無）を、後処理でも通す

## 公開

GitHub Pages で公開する。

- `docs/` 以下に静的サイトを生成する
- ナビゲーションは 篇（地獄・煉獄・天国）→ 歌 の2階層
- 原文と訳の表示切り替え、注釈のポップオーバーといった動的な要素は後回しでよい。まず全行が読める静的なページを作る
- 原文の出典（[dante-corpus](https://github.com/7shi/dante-corpus)）と、解説・訳を生成したモデル名をページに明記する

## 段階

1. 1歌分の再編集を手作業に近い形で行い、ページの形を固める
2. その工程をスクリプト化する
3. 鍵括弧の整合など後処理を組み込む
4. 地獄篇全34歌へ広げる
5. 煉獄篇・天国篇へ広げる
