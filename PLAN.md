# 構想

最終的に、**『神曲』の原文全行に注釈を埋め込んだ読み物を GitHub Pages で公開する**ことを目指す。

## 現状

生成物はカントごとに2種類ある。

- `<NN>.md` — 解説記事。場面ごとに見出しを立て、要点となる行を引用して寓意・背景を論じる
- `<NN>.txt` — 翻訳テキスト。全行について原文各行に対応する日本語訳を1行ずつ並べたもの（未訳行は空行）

この2つは補い合う関係にあるが、ファイルが分かれているために不便がある。

- **解説記事**: 引用が抜粋であるため、通読には向かない。行の被覆率もモデルによって大きく異なる（地獄篇第1歌では astra が136行中73行、gemma4-26b は13行）
- **翻訳テキスト**: 全行の訳文があるものの、なぜその訳語を選んだのかは解説を見なければ分からない。実際、第1歌17行の `raggi del pianeta`（天体の光）や118行の `color che son contenti nel foco`（苦しみを受け入れている者たち）などは、解説の注釈と対で読んで初めて意図が理解できる
- **読者体験**: 結果として、読者は2つのファイルを行き来する必要がある

## 目指す形

カントごとに1ページ。**原文全行を通して読める本文**を軸に、そこへ注釈を埋め込む。

原文（コーパス）と日本語訳（`.txt`）を横に並べた対訳レイアウトとする。行番号・原文・訳を1行として、原文が左、訳が右に来る2カラム。行番号はアンカーにして相互参照できるようにする（他のカントからの言及、解説からの参照など）。

ページ全体は、この対訳を行範囲で区切った `div` を縦に積んだ構造になる。区切りは**注釈の有無**で決める。

```
┌──────────────────────────────────────────────┐
│ 1 Nel mezzo del cammin…   人生の道の半ばで、 │  注釈のある範囲
│ 2 mi ritrovai per…        ある暗い森の中に…  │
│ 3 ché la diritta via…     まっすぐな道が…    │
├──────────────────────────────────────────────┤
│ ## 人生の半ばの「暗い森」                    │
│ 冒頭で重要なのは…（寓意・背景の解説）        │
└──────────────────────────────────────────────┘
┌──────────────────────────────────────────────┐
│ 4 Ahi quanto a dir…       ああ、どのような…  │  注釈のない範囲
│ 5 esta selva selvaggia…   あの荒々しく…      │  （対訳のみ）
│ 6 che nel pensier…        思い返すだけで…    │
└──────────────────────────────────────────────┘
```

- **注釈のある範囲**: 対訳に続けて見出しと解説を持つ
- **注釈のない範囲**: 対訳だけを淡く表示して読み流せるようにする
- **語句注**: 語句レベルの注釈は該当行に紐づける（`veltro`、`tra feltro e feltro`、`sub Iulio` など）

**範囲はカント全体を隙間なく覆う。** 解説が引用しているのは飛び飛びの行なので、注釈のある範囲どうしの間が空く。その間は注釈のない範囲として連続的に補完し、どの行も必ずいずれかの `div` に属するようにする。これにより、注釈を追う読み方と、原文を通読する読み方の両方が同一ページで成立する。

## 再編集の工程

1. 解説記事から見出しと解説段落を取り出し、それぞれが担当する行範囲を引用行番号から決定する
2. 1で埋まらなかった行を、注釈のない範囲として補完する。隣接する未割当行をまとめ、カント全体が隙間なく範囲に分割された状態にする
3. 各範囲について、コーパスの原文と `.txt` の訳文による対訳、および（あれば）見出し・解説を持つ `div` を組み立てる
4. 語句注を抽出し、該当行に紐づける

※ 注釈のない範囲が長く続く箇所は、その範囲の解説を追加生成して埋めることもできる。ただし解説を全行に付けるのが目的ではないため、必須ではない。

## 後処理

生成時ではなく、後処理で補正すべき課題がある。カント単位で1行ずつ翻訳する方式では、行を跨いだ情報が保持できないためである。

### 鍵括弧の整合

台詞の範囲は複数行にわたるため、1行単位の翻訳では開始と終了が保持されない。実際、地獄篇第1歌の astra 出力では93行で開いた台詞が96行末で閉じられているが、ウェルギリウスの発言は111行まで続く。67〜78行も一続きの発言でありながら75行で一度閉じている。

当初は原文の `«»` を数える機械的（ルールベース）な処理を構想したが、語順の違いによる改行跨ぎ（例: 第1歌65〜66行）と誤りの区別がつかないため断念した。現在はセグメント単位で原文と訳文をモデルに提示し、鍵括弧のみを補正させる [`scripts/fix_txt.py`](scripts/fix_txt.py) として実装済みである。

### その他の候補

- **隣接行の重複検出**: 同じ内容が2行以上に繰り返される崩れ方は、訳文どうしの類似度で検出できる。小さいモデルほど頻発する
- **用語の統一**: 固有名詞の表記揺れ（ヴェルトロ／ヴォルトロなど）をカントを跨いで統一する
- **未訳・複写の検査**: 現在 [`scripts/generate.py`](scripts/generate.py) が行っている検査（プレースホルダーの残存、原文の複写、日本語文字の有無）を、後処理パイプラインでも通す

## 公開

GitHub Pages で公開する。

- `docs/` 以下に静的サイトを生成する
- ナビゲーションは 篇（地獄篇・煉獄篇・天国篇）→ カント の2階層
- 原文と訳の表示切り替え、注釈のポップオーバーといった動的な要素は後回しでよい。まず全行が読める静的なページを作る
- 原文の出典（[dante-corpus](https://github.com/7shi/dante-corpus)）と、解説・訳を生成したモデル名をページに明記する

## 段階

1. 1カント分の再編集を手作業に近い形で行い、ページの形を固める
2. その工程をスクリプト化する
3. 鍵括弧の整合など後処理を組み込む（鍵括弧補正は `scripts/fix_txt.py` として先行実装済み）
4. 地獄篇全34カントへ広げる
5. 煉獄篇・天国篇へ展開する
